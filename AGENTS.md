# AGENTS.md

Read `README.md` for the product boundary and `DECISIONS.md` before a
structural change. The canonical validation command is `just check`.

Keep the default experience terminal-first and immediately visible. Every
external command or network request must have a deadline, and an unavailable
platform capability must not abort unrelated diagnostics. Passive neighbor
cache inspection must never become an active scan without a new decision.
