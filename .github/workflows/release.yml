name: release

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: Publish the already-owned current main package before tagging
        required: true
        default: check-only
        type: choice
        options:
          - check-only
          - publish-crate
  push:
    tags:
      - "linktop-v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  gate:
    name: Release contract
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      version: ${{ steps.contract.outputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Rust 1.88
        run: |
          rustup set profile minimal
          rustup toolchain install 1.88 --no-self-update
      - name: Validate package and release ref
        id: contract
        env:
          RELEASE_CONFIRM: ${{ inputs.confirm }}
          RELEASE_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          metadata="$(cargo +1.88 metadata --no-deps --format-version 1)"
          version="$(jq -er '
            if (.packages | length) == 1 and .packages[0].name == "linktop"
            then .packages[0].version
            else error("expected exactly one linktop package")
            end
          ' <<<"$metadata")"
          jq -e '
            .packages[0].publish == ["crates-io"] and
            .packages[0].rust_version == "1.88" and
            .packages[0].license == "MIT" and
            .packages[0].repository == "https://github.com/arclabs561/linktop"
          ' <<<"$metadata" >/dev/null
          cargo +1.88 metadata --locked --format-version 1 |
            jq -e '
              [.resolve.nodes[] as $node
                | .packages[]
                | select(.id == $node.id and .name == "netbraid")
                | $node.features] == [["scenario-fixtures"]]
            '
          release_tag="linktop-v${version}"
          if [[ "$RELEASE_REF" == refs/tags/* || "$RELEASE_CONFIRM" == publish-crate ]]; then
            if tagged_commit="$(git rev-parse --verify --quiet "${release_tag}^{commit}")"; then
              if [[ "$tagged_commit" != "$GITHUB_SHA" ]]; then
                printf 'version %s is already owned by %s at %s; bump before release\n' \
                  "$version" "$release_tag" "$tagged_commit" >&2
                exit 1
              fi
            fi
          fi
          if [[ "$RELEASE_REF" == refs/tags/* ]]; then
            expected_ref="refs/tags/${release_tag}"
            if [[ "$RELEASE_REF" != "$expected_ref" ]]; then
              printf 'release ref %s does not match package version %s\n' \
                "$RELEASE_REF" "$version" >&2
              exit 1
            fi
            if ! git merge-base --is-ancestor "$GITHUB_SHA" refs/remotes/origin/main; then
              printf 'release commit %s is not reachable from origin/main\n' \
                "$GITHUB_SHA" >&2
              exit 1
            fi
          fi
          printf 'version=%s\n' "$version" >>"$GITHUB_OUTPUT"
      - name: Audit locked dependencies
        run: |
          cargo +1.88 install cargo-audit --locked --version 0.22.2
          cargo audit --file Cargo.lock -D warnings
      - name: Verify crates.io package inventory
        run: |
          set -euo pipefail
          cargo +1.88 package --locked --list >"$RUNNER_TEMP/linktop-package.txt"
          grep -Fqx README.md "$RUNNER_TEMP/linktop-package.txt"
          grep -Fqx LICENSE "$RUNNER_TEMP/linktop-package.txt"
          grep -Fqx CHANGELOG.md "$RUNNER_TEMP/linktop-package.txt"
          grep -Fqx src/capture/fixtures/v1/qa_capture_manifest.json "$RUNNER_TEMP/linktop-package.txt"
          grep -Fqx src/output/fixtures/v1/snapshot.json "$RUNNER_TEMP/linktop-package.txt"
          grep -Fqx src/review/fixtures/positive-records.jsonl "$RUNNER_TEMP/linktop-package.txt"
          if grep -Eq '^(\.github/|AGENTS\.md$|docs/|justfile$)' "$RUNNER_TEMP/linktop-package.txt"; then
            echo "package contains repository-only files" >&2
            exit 1
          fi
          cargo +1.88 package --locked
          cargo +1.88 test --locked \
            --manifest-path "target/package/linktop-${{ steps.contract.outputs.version }}/Cargo.toml"

  build:
    name: Build ${{ matrix.target }}
    needs: gate
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - runner: macos-15-intel
            target: x86_64-apple-darwin
          - runner: macos-15
            target: aarch64-apple-darwin
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install Rust 1.88
        run: |
          rustup set profile minimal
          rustup toolchain install 1.88 --no-self-update
      - name: Verify native release target
        env:
          RELEASE_TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          actual_target="$(rustc +1.88 -vV | awk '/^host: / { print $2 }')"
          if [[ "$actual_target" != "$RELEASE_TARGET" ]]; then
            printf 'runner host %s does not match release target %s\n' \
              "$actual_target" "$RELEASE_TARGET" >&2
            exit 1
          fi
      - name: Build locked release binary
        run: cargo +1.88 build --locked --release --bin linktop
      - name: Package release
        env:
          RELEASE_VERSION: ${{ needs.gate.outputs.version }}
          RELEASE_TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          package="linktop-v${RELEASE_VERSION}-${RELEASE_TARGET}"
          mkdir -p "dist/${package}"
          cp target/release/linktop "dist/${package}/linktop"
          cp README.md LICENSE CHANGELOG.md "dist/${package}/"
          tar -czf "dist/${package}.tar.gz" -C dist "$package"
          mkdir -p verify
          tar -xzf "dist/${package}.tar.gz" -C verify
          "verify/${package}/linktop" --version
          "verify/${package}/linktop" --help >/dev/null
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linktop-release-${{ matrix.target }}
          path: dist/*.tar.gz
          if-no-files-found: error
          retention-days: 14

  bundle:
    name: Bundle dry-run assets
    needs:
      - gate
      - build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: linktop-release-*
          path: dist
          merge-multiple: true
      - name: Generate and verify checksums
        run: |
          set -euo pipefail
          cd dist
          shasum -a 256 linktop-v*.tar.gz >SHA256SUMS
          shasum -a 256 --check SHA256SUMS
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linktop-release-assets
          path: |
            dist/linktop-v*.tar.gz
            dist/SHA256SUMS
          if-no-files-found: error
          retention-days: 14

  crates:
    name: Publish crates.io package
    if: |
      (github.event_name == 'workflow_dispatch' && inputs.confirm == 'publish-crate') ||
      (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/linktop-v'))
    needs:
      - gate
      - bundle
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install Rust 1.88
        run: |
          rustup set profile minimal
          rustup toolchain install 1.88 --no-self-update
      - name: Require current main and green CI before manual publication
        if: github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git fetch --no-tags origin refs/heads/main:refs/remotes/origin/main
          if [[ "$(git rev-parse HEAD)" != "$(git rev-parse refs/remotes/origin/main)" ]]; then
            printf 'manual publication commit %s is not current origin/main %s\n' \
              "$(git rev-parse HEAD)" \
              "$(git rev-parse refs/remotes/origin/main)" >&2
            exit 1
          fi
          conclusion="$(
            gh run list \
              --repo "$GITHUB_REPOSITORY" \
              --workflow ci.yml \
              --event push \
              --commit "$GITHUB_SHA" \
              --limit 1 \
              --json conclusion \
              --jq '.[0].conclusion // "missing"'
          )"
          if [[ "$conclusion" != success ]]; then
            printf 'CI for %s is %s; refusing crates.io publication\n' \
              "$GITHUB_SHA" "$conclusion" >&2
            exit 1
          fi
      - name: Require existing crate ownership for Trusted Publishing
        if: github.event_name == 'workflow_dispatch'
        run: |
          set -euo pipefail
          package=linktop
          if ! http_code="$(
            curl --silent --location --connect-timeout 10 --max-time 30 \
              --output /dev/null --write-out '%{http_code}' \
              --user-agent 'linktop-release-workflow (https://github.com/arclabs561/linktop)' \
              "https://crates.io/api/v1/crates/$package"
          )"; then
            printf 'crates.io ownership lookup for %s failed before an HTTP response\n' \
              "$package" >&2
            exit 1
          fi
          case "$http_code" in
            200)
              ;;
            404)
              printf '%s has no crates.io owner; Trusted Publishing cannot bootstrap it\n' \
                "$package" >&2
              printf 'publish from a clean current-main checkout with one scoped token, configure release.yml as the trusted publisher, revoke the token, then rerun\n' >&2
              exit 1
              ;;
            *)
              printf 'crates.io ownership lookup for %s returned HTTP %s\n' \
                "$package" "$http_code" >&2
              exit 1
              ;;
          esac
      - uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5
        if: github.event_name == 'workflow_dispatch'
        id: auth
      - name: Publish package
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
          RELEASE_EVENT: ${{ github.event_name }}
          RELEASE_VERSION: ${{ needs.gate.outputs.version }}
        run: |
          set -euo pipefail
          registry_version_matches_release() {
            local package="$1"
            local metadata
            metadata="$(
              curl --fail --silent \
                --user-agent 'linktop-release-workflow (https://github.com/arclabs561/linktop)' \
                "https://crates.io/api/v1/crates/$package/$RELEASE_VERSION"
            )" || return 1
            printf '%s' "$metadata" | jq -e \
              --arg package "$package" \
              --arg version "$RELEASE_VERSION" \
              '
                .version.crate == $package and
                .version.num == $version and
                .version.repository == "https://github.com/arclabs561/linktop" and
                .version.yanked == false
              ' >/dev/null || return 1
            curl --fail --silent --location \
              --user-agent 'linktop-release-workflow (https://github.com/arclabs561/linktop)' \
              "https://crates.io/api/v1/crates/$package/$RELEASE_VERSION/download" |
              tar -xzO "$package-$RELEASE_VERSION/.cargo_vcs_info.json" |
              jq -e --arg sha "$GITHUB_SHA" \
                '.git.sha1 == $sha and (.git.dirty // false) == false' >/dev/null
          }
          wait_for_registry_version() {
            local package="$1"
            for _ in {1..24}; do
              if registry_version_matches_release "$package"; then
                return 0
              fi
              sleep 5
            done
            printf 'published %s %s but crates.io did not expose it within 120 seconds\n' \
              "$package" "$RELEASE_VERSION" >&2
            return 1
          }
          package=linktop
          if registry_version_matches_release "$package"; then
            printf '%s %s from this release commit is already present on crates.io\n' \
              "$package" "$RELEASE_VERSION"
            exit 0
          fi
          if [[ "$RELEASE_EVENT" != workflow_dispatch ]]; then
            printf '%s %s is absent or does not match release commit %s\n' \
              "$package" "$RELEASE_VERSION" "$GITHUB_SHA" >&2
            printf 'publish with the confirmed manual workflow before pushing the tag\n' >&2
            exit 1
          fi
          cargo +1.88 publish --locked --dry-run
          cargo +1.88 publish --locked
          wait_for_registry_version "$package"

  publish:
    name: Publish GitHub release
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/linktop-v')
    needs:
      - gate
      - bundle
      - crates
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      attestations: write
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: linktop-release-assets
          path: dist
      - name: Revalidate tag and checksums
        env:
          RELEASE_TAG: ${{ github.ref_name }}
          RELEASE_VERSION: ${{ needs.gate.outputs.version }}
        run: |
          set -euo pipefail
          if [[ "$RELEASE_TAG" != "linktop-v${RELEASE_VERSION}" ]]; then
            printf 'release tag %s does not match package version %s\n' \
              "$RELEASE_TAG" "$RELEASE_VERSION" >&2
            exit 1
          fi
          git fetch --force --no-tags origin \
            "refs/tags/${RELEASE_TAG}:refs/tags/${RELEASE_TAG}"
          workflow_commit="$(git rev-parse "${GITHUB_SHA}^{commit}")"
          remote_tag_commit="$(git rev-parse "refs/tags/${RELEASE_TAG}^{commit}")"
          if [[ "$remote_tag_commit" != "$workflow_commit" ]]; then
            printf 'remote tag resolves to %s, workflow built %s\n' \
              "$remote_tag_commit" "$workflow_commit" >&2
            exit 1
          fi
          cd dist
          shasum -a 256 --check SHA256SUMS
      - name: Attest release assets
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-path: dist/*
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
          RELEASE_VERSION: ${{ needs.gate.outputs.version }}
        run: |
          gh release create "$RELEASE_TAG" dist/* \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --title "Linktop ${RELEASE_VERSION}" \
            --generate-notes
